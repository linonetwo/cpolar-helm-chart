apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cpolar.fullname" . }}-test-connection"
  labels:
    {{- include "cpolar.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cpolar.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
